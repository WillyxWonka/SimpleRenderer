# cmake
cmake_minimum_required(VERSION 3.20)

project(SimpleRenderer LANGUAGES C CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

include(FetchContent)

FetchContent_Declare(
    glfw
    GIT_REPOSITORY https://github.com/glfw/glfw.git
    GIT_TAG 3.4
)
FetchContent_Declare(
    glm
    GIT_REPOSITORY https://github.com/g-truc/glm.git
    GIT_TAG 1.0.1
)

set(GLFW_BUILD_DOCS OFF CACHE BOOL "" FORCE)
set(GLFW_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(GLFW_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)

FetchContent_MakeAvailable(glfw glm)

add_library(glad
    external/glad/src/gl.c
)

target_include_directories(glad
    PUBLIC
        external/glad/include
)

add_executable(SimpleRenderer
    src/main.cpp
)

target_link_libraries(SimpleRenderer
    PRIVATE
        glfw
        glad
        glm::glm
)

# Copy runtime assets next to the executable after building.
add_custom_command(
    TARGET SimpleRenderer POST_BUILD

    COMMAND ${CMAKE_COMMAND} -E copy_directory
        "${PROJECT_SOURCE_DIR}/assets"
        "$<TARGET_FILE_DIR:SimpleRenderer>/assets"
)